import { Routes } from '@angular/router';
import { AreaIndisponivelComponent } from './area-indisponivel.component';

export const routes: Routes = [
  {
    path: 'area-indisponivel',
    pathMatch: 'full',
    component: AreaIndisponivelComponent,
  }
];
